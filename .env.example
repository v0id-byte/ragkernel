# LLM provider 密钥。默认走 MiniMax（见 config/settings.yaml 的 provider）。
MINIMAX_API_KEY=
# 切官方 Claude 时改用：把 settings.yaml 的 provider.base_url 留空、model 设 claude-sonnet-5、
# api_key_env 设 ANTHROPIC_API_KEY，并在此填：
# ANTHROPIC_API_KEY=
